# wb-mqtt-urri tests

The suite checks configuration validation and the MQTT lifecycle without a real broker or URRI receiver.

Run it with the same Python checks as Jenkins:

```shell
python3 -m pytest tests
```
